(declare-const p Bool)
(declare-const q Bool)
(declare-const r Bool)

(push)
    (assert(=> (=> p q) (not p)))
    (check-sat)
(pop)

(push)
    (assert (and (or p q) 
        (=> p q) 
        (or (not q) p) 
        (not (and p q))))
        (check-sat)
(pop)

(push)
    (assert(and (or (not p) (not q))
            (or (not p) r)
            (or (not q) r)
    ))
    (check-sat)
(pop)